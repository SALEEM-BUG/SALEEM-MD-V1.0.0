name: 𝐒𝐀𝐋𝐄𝐄𝐌-𝐌𝐃
services:
  - name: Saleem-MD-service
    type: web
    ports:
      - 5000
    routes:
      - path: "/"
    build:
      type: docker
      dockerfile: ./Dockerfile
    regions:
      - fra
      - sin
      
